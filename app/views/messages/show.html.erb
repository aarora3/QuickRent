<h2> <%= @message.title %></h2>
<p> <%= @message.description %></p>

<!-- Add a posted timestamp -->
<%= link_to "Back to home page", root_path %>
<br> <br>

<%= link_to "Edit Message", edit_message_path(@message) %>
<br> <br>
<%= link_to "Delete Message", message_path(@message), method: :delete, data: {confirm: "Are you sure you want to delete the message?"} %>